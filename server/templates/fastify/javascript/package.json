{
  "name": "{{PROJECT_NAME}}",
  "version": "1.0.0",
  "description": "Backend API server generated by create-node-server",
  "private": true,
  "type": "module",

  "engines": {
    "node": ">=20"
  },

  "scripts": {
    "dev": "node --watch src/index.js",
    "start": "node src/index.js",

    "lint": "eslint .",
    "lint:fix": "eslint . --fix",

    "format": "prettier . --write",
    "format:check": "prettier . --check"
  },

  "dependencies": {
    "fastify": "^5.0.0",
    "dotenv": "^17.2.0"
  },

  "devDependencies": {
    "eslint": "^9.31.0",
    "@eslint/js": "^9.31.0",
    "eslint-config-prettier": "^10.1.5",

    "prettier": "^3.6.2"
  }
}